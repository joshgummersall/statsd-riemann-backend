image: node:5

before_script:
  - apt-get update -qq && apt-get install -y -qq libprotobuf-dev
  - npm install

lint:
  script: "npm run lint"

build:
  script: "npm run build"

test:
  script: "npm test"
